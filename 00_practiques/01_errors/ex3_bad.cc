#include <iostream>
#include <cmath>
using namespace std;

// Nombre en aritmètica de 3 dígits
struct NA3 {
    int n;
    int e;

    NA3(int n_o, int e_o) {
        n = n_0;
        e = e_0;
    };

    NA3();
}

int pow_n

NA3 suma(NA3 &x, NA3 &y) {
    NA3 result;
    if (x.e > y.e) {
        result.n = x.n + y.n*(pow_n(10, x.e) - pow_n(10, y.e));
    }
}

typedef NUMBER double;

NUMBER tall(NUMBER x) {
    
}

int main() {
    
}
